# Copy to .env. Keys stay on the backend and must never use a VITE_ prefix.
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=
OPENAI_API_KEY=
# Leave empty for provider default; override for a model enabled in your account.
LLM_MODEL=
LLM_TIMEOUT_SECONDS=8
AEGIS_OFFLINE=true
